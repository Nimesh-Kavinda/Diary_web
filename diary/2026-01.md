# Developer Diary — January 2026

---

## 2026-01-01

> New Year's Day — Public Holiday

---

## 2026-01-02

- First working day of 2026. Fixed the **Content Security Policy** in the WearIt extension manifest to include `data:` and `blob:` URI sources — these were needed for displaying base64-encoded clothing images and object URLs created by the extension's image processor. Enhanced button styles in the Login and Signup pages. Standardised code formatting across the History, MyWardrobe, and MyProfile components.

---

## 2026-01-03

> Weekend

---

## 2026-01-04

> Weekend

---

## 2026-01-05

- Feature-heavy WearIt development day. Added the `apiFetch` utility for all extension API calls, providing a consistent wrapper around `fetch()` with auth headers. Added new dashboard routes and their corresponding page components. Enhanced QuickView and AiTryOn components for a smoother clothing selection experience — image selection now persists across tab navigation. Implemented **clothing detection in the content script**: DOM traversal with a vocabulary-based scoring system that identifies clothing images by inspecting alt text, class names, and surrounding context text. Fixed a local storage login details issue that was breaking session continuity on extension reload.

---

## 2026-01-06

- Refined the clothing detection scoring logic with an expanded vocabulary set covering more clothing item types. Improved the image scoring algorithm to weight image dimensions (larger images score higher as they're more likely to be the main product image). Implemented the **ScrollToTop** component for in-extension navigation. Added `chrome.storage.onChanged` listeners in App and AiTryOn to trigger real-time navigation and image updates when storage values change. Polished the WearIt try-on button styling and improved image processing for search results pages.

---

## 2026-01-07

- Wrote the **comprehensive project README** for WearIt-extension with features, tech stack, installation instructions, and a screenshot.
- Also: **Initialized and completed the ultimatewebscraper landing page** in the same day. Built the full page: Navbar, Features component with feature video embeds, Roadmap section, Testimonials, UseCases, ThemeToggle, and VideoPlaceholder components. Implemented dark mode support with a custom ThemeContext, animated SectionDivider components with framer-motion spring effects, and polished the Hero, Navbar, and Features visual hierarchy.

---

## 2026-01-08

- Reviewed the ultimatewebscraper landing page across browsers and fixed minor visual inconsistencies. Tested the dark mode toggle and refined the ThemeContext initialisation logic to correctly read `localStorage` on first load. Planned SEO metadata improvements and Open Graph tags.

---

## 2026-01-09

- Launched the **Pinterest board image downloader** Chrome extension project with a full feature set shipped on day one:
  - Custom browser folder selection dialog for download destination
  - Bulk download progress bar with live percentage updates
  - Individual download button injected onto every pin image
  - Download completion toast notification
  - Browser security policy note displayed when folder selection is used
  - Fixed a CSV metadata file data issue (wrong column ordering)
  - Cleaned up the repository by removing unnecessary build artefacts
  - Implemented both individual pin download and popup-based bulk download
  - Built the metadata-only download mode for entire board pages (CSV export without downloading images)

---

## 2026-01-10

> Weekend

---

## 2026-01-11

- Weekend work on the Pinterest board downloader. Enhanced the download button functionality for both the user's own boards and other users' boards — the injection logic needed to be different depending on which type of board page was being viewed. Refactored the 'other user board' download button injection to position next to the board name element instead of the toolbar.

---

## 2026-01-12

- Reviewed Pinterest pin data analytics requirements and planned the new extension architecture. Studied the Pinterest DOM structure across board layouts, search results, and home feed to identify reliable data extraction points. Drafted the analytics overlay design — a floating badge on each pin card showing saves, comments, and media type.

---

## 2026-01-13

- Initialized the **Pinterest pin data analytics Chrome extension** and shipped real functionality on day one. Built the analytics data extraction from the live Pinterest DOM — pulling real save counts, comment counts, and pin metadata. Fixed a video pin fetching issue where video thumbnails were not being captured. Implemented a scrolling function with a 0.5-second wait time to allow lazy-loaded pins to render before scraping.

---

## 2026-01-14

> Duruthu Full Moon Poya Day — Public Holiday

---

## 2026-01-15

- Tested the analytics overlay across multiple Pinterest board layouts (grid, compact, full-width). Investigated DOM selector reliability — Pinterest frequently updates its class names. Added fallback selector logic for missing pin data fields. Planned the **Sort Filter feature** UI and identified the correct injection point in the Pinterest search/board header.

---

## 2026-01-16

- Added a visual guide documentation for the Sort Filter feature (later removed in the same session as it was no longer needed in the final UX). Deleted all outdated documentation and unused image files to keep the repository clean. Enhanced the **SortPin** functionality with a proper sorting UI, analytics-based sorting (sort by saves, comments, media type), and improved analytics integration. Refactored `content-script.js` for readability and consistent variable naming.

---

## 2026-01-17

> Weekend

---

## 2026-01-18

> Weekend

---

## 2026-01-19

- Debugged a SortPin timing issue where the sort button was appearing before the Pinterest pin grid had fully loaded. Implemented a **MutationObserver** approach to watch the main feed container and inject the SortPin controls only after the pin cards are present in the DOM. Prepared the export data CSV/JSON schema for the planned bulk export feature.

---

## 2026-01-20

- Built out the export data schema for pin analytics — defining column order and data types for both CSV and JSON export formats. Tested the pin card size preset feature across different board width configurations. Refined the dropdown direction options for the sort filter popup so it doesn't clip off-screen on narrow boards.

---

## 2026-01-21

- Feature-dense day for the Pinterest analytics extension. Added a **dropdown direction option** for the sort filter popup (above/below). Built the **export functionality** with selection options (selected pins vs all visible pins), ZIP packaging, and UI enhancements. Added **pin card size presets** (compact, medium, large) for dynamic resizing via a popup control. Enhanced the SortPin filter options and card layout with responsive design. Improved warm lead panel UI with better stats display. Refactored owner profile data extraction logic.

---

## 2026-01-22

- Added **media download functionality** with download progress animations and proper error handling for failed downloads. Implemented **bulk media export with ZIP support** — selecting multiple pins and downloading all their images/videos as a single `.zip` file. Added **media type detection** badges in the analytics overlay — distinguishing image pins from video pins. Enhanced media download with improved error handling and explicit support for video URL formats.

---

## 2026-01-23

- Tested media download across edge cases: private board pins, video pins from different CDN patterns, pins with missing media URLs, and pins behind login walls. Reviewed sort filter and card layout consistency across board types. Identified and noted that the comments metric was missing from pin data and that the warm leads scoring logic needed refinement.

---

## 2026-01-24

> Weekend

---

## 2026-01-25

> Weekend

---

## 2026-01-26

- Final major feature day for the analytics extension. Added the **comments metric** to the pin data model, updated the scraping logic, and integrated comment counts into all display components. Enhanced the analytics overlay with comment count badges and improved rendering for mixed media boards. Added a **custom pin count input** with validation and live updates. Implemented the **in-page sorted results view** — a proper full-page results layout injected into Pinterest instead of an overlay. Added the **Warm Leads button**. Built a complete **notification system**: context-based toast notifications with different tone configurations (success, warning, error).

---

## 2026-01-27

- Reviewed the warm leads feature with live Pinterest data — tested it on real business accounts with many pins. Fixed edge cases in the notification toast timing to prevent duplicate toasts. Began planning the **gemini_watermark_remover** project: researched Canvas API and service worker architecture for background image processing.

---

## 2026-01-28

- Researched Chrome Extension approaches for background image processing. Compared content script vs. background service worker patterns — chose service worker as it avoids blocking the page render thread. Drafted the architecture: service worker handles image downloads and PNG conversion; content script handles detection and UI injection. Studied PNG conversion via the Canvas API for cross-format image handling.

---

## 2026-01-29

- Prototyped the background service worker for image capture and PNG conversion. Tested the Gemini API prompt structure for watermark detection and removal — crafted prompts that instruct the model to identify watermark regions and describe how to mask them. Reviewed browser security constraints for `blob:` URL handling within service workers (limited compared to content scripts).

---

## 2026-01-30

- Initialized the **gemini_watermark_remover** Chrome extension and shipped the core functionality. Implemented the **background service worker** to download images and convert them to PNG format before processing. Built the core watermark removal function using the Gemini Vision API to process images and return cleaned versions. Documented all known issues and API limitations in `issue.md` — including that Gemini API image editing is not deterministic and results vary between runs.
